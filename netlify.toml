[build]
  command = "npm run build" # Change this to your SvelteKit build command
  publish = "build"         # Directory where your SvelteKit app is built


[functions]
        node_bundler = "esbuild"